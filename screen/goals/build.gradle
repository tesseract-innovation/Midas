plugins {
    id 'midas.kotlin-android'
    id 'midas.compose'
}

android {
    namespace 'com.midasmoney.screen.goals'
}

dependencies {
    implementation(project(':shared:ui:core'))
    implementation(project(':shared:model:data'))
    implementation(project(':shared:model:mock'))
    implementation(project(':shared:resource'))
}
