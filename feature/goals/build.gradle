plugins {
    id 'midas.library'
}

android {
    namespace 'com.midasmoney.feature.goals'
}

dependencies {
    implementation(project(':core:ui'))
    implementation(project(':core:data:mock'))
    implementation(project(':core:domain:model'))
    implementation(project(':core:resource'))
}
